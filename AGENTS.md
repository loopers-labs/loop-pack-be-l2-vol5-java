# Codex 작업 규칙

이 문서는 프로젝트 전체에 적용하는 AI 작업 지침이다. 아래 내용은 합의된 목표 설계이며, 기존 코드가 이미 이를 구현했다는 뜻은 아니다. 사용자 요청과 기존 계약을 먼저 확인하고 현재 작업 범위 안에서 적용한다.

## 최우선 작업 규칙 — 수정 전 승인 필수

이 규칙은 프로젝트의 다른 작업 지침보다 우선한다. 설계 합의나 일반적인 구현 요청을 개별 파일 변경안의 승인으로 간주하지 않는다.

- 프로젝트 파일을 수정하기 전에 작업을 의미 있는 변경 단위로 나누어 제안한다.
- 각 단위마다 **대상 파일, 변경 내용, 이유, 검증 방법**을 설명하고 사용자에게 명시적인 승인을 받은 뒤 수정한다.
- “적용해줘”, “수정해줘”라는 최초 요청에도 먼저 구체적인 변경안을 제시한다. 제시한 변경안에 대한 후속 승인을 실행 조건으로 삼는다.
- 승인은 제시한 변경 단위에만 적용한다. 승인된 단위는 반복 승인 없이 수행하되, 추가 변경이나 범위 확대가 필요하면 다시 승인받는다. 검증 중 발견한 문제의 수정도 승인 범위 밖이면 별도로 제안한다.
- 코드·테스트·설정·문서와 이 AGENTS.md를 포함한 파일 생성·수정·이동·삭제, 자동 포맷·자동 수정 모두 승인 대상이다.
- 읽기·검색·검토는 승인 없이 진행할 수 있다. 수정이나 파일 생성을 수반하는 명령은 제안한 변경·검증 범위에 포함해 승인받는다.
- 답변이 없거나 모호하면 승인으로 간주하지 않는다.

## 1. 작업 절차와 범위

- 현재 작업에 적용되는 최신 합의와 API 계약·기대값·패키지 의존을 따른다. 기존 계약과 관련 테스트를 확인하되, 과거 문서와 충돌하면 충돌 지점을 설명한다. 구체적으로 승인된 변경 단위는 다시 확인하지 않고, 과거 실습의 임시 범위를 현재 구현에 자동 적용하지 않는다.
- 구현 전에 최우선 작업 규칙에 따라 변경 단위별 제안과 승인을 완료한다. 승인된 범위 안에서만 구현·검증한다.
- 승인된 작은 기능 단위마다 diff와 관련 테스트·lint·ArchUnit 결과를 확인한 뒤 다음 기능으로 진행한다. 다음 변경 단위도 승인 범위에 포함되는지 확인한다.
- API의 method/path·입력·성공·대표 오류와 주요 규칙의 기대값을 `docs/week2/commerce-design.md`와 테스트에 연결한다. 구현하며 설계 판단이 바뀌면 문서 변경도 제안하고 승인받아 반영한다.
- 미정 정책은 기존 계약에 결정이 있는지 먼저 찾는다. 없다면 필요한 정책을 질문하고, 답변에 의존하지 않는 작업은 계속한다. 제안한 정책을 확정된 계약으로 취급하지 않는다.
- 금액·권한·상태 전이·중복 처리·외부 API 계약처럼 사용자 관찰 결과가 달라지는 정책은 확인한다. 메서드명, 내부 클래스 배치, 중복 제거 등 일반적인 구현 변경도 제안한 변경 단위에 포함해 승인받는다.
- 정책·검사 기준 변경이나 범위 밖 개편은 이유와 영향을 설명하고 확인을 받는다. 승인된 설계를 적용한다는 이유로 프로젝트 전체를 일괄 개편하지 않는다.
- 사용자 변경을 덮어쓰지 않는다. 요청하지 않은 의존성 업그레이드·서비스 분리·배포를 수행하지 않는다.
- 검사 통과를 위해 테스트·기대값·lint·ArchUnit 규칙을 삭제하거나 완화하지 않는다. 검사 대상 제외, 실패 무시, skip으로 문제를 숨기지 않는다.

### Example 참고 범위와 작업 제외

- Example 테스트와 기존 요구·오류 계약의 정리 방식은 참고만 한다. 정상·대표 오류 사례 구성, 입력·성공 결과·오류·실패 후 상태를 명시하는 방식을 참고해 신규 커머스 기능의 계약과 테스트를 별도로 작성한다.
- Example을 구현·확장·리팩터링 대상으로 삼거나 신규 커머스 기능이 Example 코드에 의존하도록 만들지 않는다. 예제의 업무 정책·기대값·클래스 구조를 신규 기능의 확정 계약으로 복사하지 않는다. 신규 기능에는 합의한 커머스 요구사항과 설계를 적용한다.
- 기존 Example은 starter 참고 코드다. 별도 요청이 없으면 코드·전용 테스트를 수정하거나 예제용 클래스·매핑을 추가하지 않는다. 기존 테스트는 삭제·비활성화하지 않고 전체 회귀 검사에서 유지한다.
- 기존 `..example..` 패키지와 `com.loopers.domain.BaseEntity`만 신규 헥사고날 ArchUnit 규칙에서 제외한다. 신규 커머스 코드를 예제 패키지에 넣거나 기존 JPA BaseEntity를 도메인에서 상속하지 않는다. 이 예외를 다른 구현의 검사 완화에 확대 적용하지 않는다.

## 2. 아키텍처와 의존 방향

- 순수 도메인과 JPA 영속 모델을 분리한 헥사고날 아키텍처를 적용한다.
- 기본 구조는 `interfaces / application / domain / infrastructure`이고 하나의 API 서버와 DB를 사용한다.
- `interfaces`: HTTP 입력·응답 DTO, 식별 정보 전달, 오류 매핑을 담당한다. infrastructure를 직접 호출하지 않는다.
- `application`: 도메인별 `BrandApplicationService`, `ProductApplicationService`, `PointApplicationService`, `LikeApplicationService`, `OrderApplicationService`가 실행 순서·소유권·관계 검증·트랜잭션을 조율한다.
- 도메인별 ApplicationService 하나로 시작하고 Controller가 공개 메서드를 직접 호출한다. 이 공개 메서드가 입력 포트 역할을 하며 입력·반환에는 도메인 또는 application 타입을 사용한다. 별도 `XxxUseCase` 인터페이스·Facade·Command/Query Service·Dispatcher는 두지 않는다. 내부 중복 제거 등 기존 경계를 유지하는 리팩터링도 변경 단위별 승인 후 수행한다. 새로운 계층·입력 인터페이스 도입이나 서비스 책임 경계 변경은 제안에 이유와 영향을 명시한다.
- 조회·저장을 함께 제공하는 출력 포트인 Repository는 해당 application 기능의 `port` 패키지에 둔다. application은 infrastructure 구현과 Controller DTO를 참조하지 않는다.
- `domain`: 순수 Java Entity·VO·업무 규칙만 둔다. application·interfaces·infrastructure·Spring·JPA·HTTP에 의존하지 않는다.
- `infrastructure`: 출력 포트를 구현하고 JPA Entity·쿼리·도메인 변환을 관리한다. HTTP 응답·권한·판매 정책을 결정하지 않는다. 도메인별 PersistenceAdapter로 시작한다. 각 어댑터가 해당 도메인의 Repository를 구현해 조회·저장을 함께 처리한다. 모든 도메인을 하나의 어댑터로 합치거나 메서드마다 어댑터를 만들지 않는다. 단순 변환은 어댑터 내부 메서드로 시작하며 별도 Mapper 클래스를 일괄 생성하지 않는다.
- `config`: 실행 환경과 객체 연결을 담당한다. 업무 규칙을 넣지 않는다.
- domain 내부의 ID 타입 참조는 허용한다. Entity가 Repository를 직접 호출하지 않게 한다.
- application의 Spring 트랜잭션 사용은 허용한다. 필요한 출력 포트를 직접 주입한다. 주문 확정은 OrderApplicationService가 Order·Product·Point의 협력을 직접 조율하며, 전달만 하는 Facade·Service 호출 단계를 추가하지 않는다.
- 포트에는 도메인 또는 application 타입을 사용한다. JPA Entity, HTTP DTO, Spring Data `Page`를 노출하지 않는다.
- 도메인 오류는 HTTP 상태 코드에 의존하지 않는다. interfaces가 확정된 오류 계약으로 변환한다. 예상하지 못한 예외를 일괄적으로 입력 오류로 처리하거나 내부 예외 내용을 응답에 노출하지 않는다.

## 3. Entity·VO와 애그리거트

독립 애그리거트 루트는 `Brand`, `Product`, `Point`, `Like`, `Order`다. 기존 도메인과 Entity·VO의 역할은 유지한다.

- Entity·VO는 `domain.brand`, `domain.product`, `domain.like`, `domain.point`, `domain.order` 중 소속 도메인에 함께 둔다. 별도의 공통 `entity`·`vo` 패키지로 분리하지 않는다. BrandId는 brand, ProductId·Stock은 product, OrderItem·Quantity는 order에 둔다.
- 공유 금액 VO는 `domain.common.Money`에 둔다. long의 0 이상 범위를 사용하고 연산 범위 초과를 거절한다.

| 루트 | 정체성·내부 구성 |
| --- | --- |
| Brand | 브랜드 ID, 브랜드 정보·삭제 상태 |
| Product | 상품 ID, BrandId, Money, Stock, 삭제 상태 |
| Point | 사용자 ID, 잔액 Money |
| Like | 사용자 ID와 상품 ID의 조합으로 식별하는 관계 |
| Order | 주문 ID, 구매자 ID, OrderItem 목록, 합계·상태·결제액·결과 |

- `BrandId`, `ProductId`, `Money`, `Quantity`, `Stock`, `OrderItem`은 불변 VO로 설계한다. 값 변경은 새 객체로 교체한다.
- Money는 0 이상, Quantity는 양수, Stock은 0 이상을 보장한다. 이름·설명 등 모든 필드를 별도 VO로 감싸지 않는다. 단순한 규칙은 Entity의 행동에 두고 도메인 서비스를 일괄 추가하지 않는다.
- 금액에 부동소수점을 사용하지 않고 덧셈·곱셈의 표현 범위 초과를 검사한다.
- 애그리거트 변경은 루트의 행동을 통한다. 임의 setter나 수정 가능한 내부 컬렉션을 노출하지 않는다.
- Order가 품목과 합계를 관리한다. 외부 입력 합계를 신뢰하지 않고 서버가 조회한 단가와 수량으로 계산한다.
- 현재 OrderItem은 독립적인 수정·배송·취소가 없는 VO다. 저장용 JPA 행의 ID 때문에 도메인 Entity로 바꾸지 않는다.
- Product는 BrandId만 보관하고 Brand는 상품 컬렉션을 갖지 않는다. 다른 애그리거트도 ID로 참조한다.
- 도메인 객체는 JPA 관리 객체가 아니므로 변경 후 Repository로 명시적으로 저장한다. DB에서 복원할 때는 저장된 ID·상태·금액을 복원하고 신규 생성·충전·확정 행동을 재실행하지 않는다. DRAFT 가격은 생성 시 단가로 고정하며 확정 시 재산정하지 않는다.
- Repository는 루트 단위로 둔다. application에 StockRepository나 OrderItemRepository를 노출하지 않는다.
- CQRS를 적용하지 않는다. 도메인별 ApplicationService와 Repository가 조회·변경을 함께 처리하며 별도 조회 전용 포트·서비스를 두지 않는다.
- 고객 상품 목록·상세와 변경 대상은 ProductRepository에서 Product로 조회한다. ProductApplicationService가 BrandRepository의 브랜드 조회와 LikeRepository의 좋아요 수 집계 결과를 조합해 application 결과 DTO를 만든다. 목록에서는 브랜드·좋아요 수를 ID 목록으로 일괄 조회한다. Controller는 결과를 HTTP 응답으로 변환한다.
- Product에 응답 조합을 위한 브랜드명·좋아요 수를 추가하지 않는다. 상태 변경은 Product의 행동을 호출한 뒤 Repository로 저장한다. 결과 DTO로 상태를 변경하거나 저장하지 않는다.
- 좋아요 순 상품 검색은 ProductPersistenceAdapter가 DB에서 JOIN·집계·정렬한 뒤 페이지 처리하며 Product 목록을 반환한다. 페이지를 가져온 뒤 application에서 좋아요 순으로 재정렬하지 않는다. SQL의 테이블 참조를 도메인 객체 간 연관관계로 옮기지 않는다.
- 목록의 필터·정렬·페이지 처리는 저장소 조회에서 수행한다. 전체 데이터를 읽어 메모리에서 페이지 처리하거나 목록의 각 항목마다 추가 조회하는 구현을 피한다.

## 4. 업무 규칙

### 브랜드·상품·삭제

- 상품 생성 시 application이 브랜드 존재·활성 상태를 확인한다. 상품 수정에서는 기존 브랜드를 유지한다.
- 브랜드 삭제 전 연결된 미삭제 상품의 존재를 확인한다. 재고 0인 상품도 삭제 차단 대상이다.
- 브랜드·상품은 논리 삭제한다. 고객 조회에서 제외한다. 과제 요구에 따라 이미 삭제된 브랜드·상품의 수정과 삭제된 상품의 재고 변경을 거절한다.
- 없거나 삭제된 브랜드·상품의 고객 상세 조회는 모두 없는 대상 오류로 처리한다. 구체적인 HTTP 상태·오류 코드는 확정된 계약을 따른다.
- 삭제 상품은 새 주문·주문 확정·새 좋아요의 대상으로 사용하지 않는다.
- 브랜드·상품 삭제가 상품·주문 품목의 연쇄 삭제로 이어지지 않게 한다.
- 관리자 재고 변경은 0 이상인 최종 수량 설정이며 Product 루트를 통해 수행한다.

### 좋아요

- 사용자–상품 관계를 저장하고 DB에 `(user_id, product_id)` 유일 제약을 둔다.
- 좋아요 수는 관계에서 계산한다. 별도의 카운터를 기본 설계로 도입하지 않는다.
- 내 목록에서 삭제 상품을 제외한다. 삭제 상품에 남은 자신의 관계는 취소할 수 있다.
- 취소 시 활성 상품 존재를 필수 조건으로 만들지 않는다.

### 포인트·주문

- 1포인트는 1원이다. 충전 입력 누락·잘못된 타입·표현 범위 초과·0 이하 금액을 거절한다.
- 잔액 0은 허용한다. 충전·결제 실패 시 기존 잔액을 유지한다.
- 주문 생성 시 상품 존재·활성 상태와 양수 수량을 확인하고 품목·단가·합계를 DRAFT로 저장한다. 생성 시 차감하지 않는다.
- 현재 임시 정책은 중복 주문 품목 거절이다. 각 상품별 수량으로 재고를 확인한다.
- 최초 주문 확정은 본인의 DRAFT 주문에 대해서만 수행한다. 이미 CONFIRMED인 주문의 재요청은 409로 거절하고 상태를 유지한다.
- 확정은 재고 차감, 포인트 차감, CONFIRMED 전이, 결제액·결과 저장을 하나의 DB 트랜잭션에서 수행한다. 하나라도 실패하면 전체를 롤백한다.
- 여러 루트를 함께 변경한다는 이유로 Product·Point를 Order 애그리거트에 포함하지 않는다. 재고·잔액 계산과 상태 규칙은 Entity·VO에 두고 application에 중복 작성하지 않는다.
- 단일 트랜잭션의 전체 롤백과 동시 요청 제어는 구분한다. 현재 구현은 주문 → 상품 ID 오름차순 → 포인트 순서로 DB 행을 잠그며, 기본 기능을 위해 이벤트·분산 트랜잭션·별도 조회 DB를 도입하지 않는다.

## 5. 고객·관리자 경계

- 고객은 `/api/v1/**`, 관리자는 `/api-admin/v1/**`를 사용한다. Controller와 요청·응답 DTO를 분리한다.
- 고객은 기존 로컬 실습용 사용자 식별 계약을 따르며, 식별 누락·존재하지 않는 사용자를 거절한다. 고객 소유권 검증과 함께 정상·거절 사례를 확인한다.
- 관리자 기능은 브랜드·상품 CRUD, 재고 설정, 구매자별 주문 목록·상세 조회다.
- 관리자 역할은 Controller 진입 전 Spring Security 경계에서 확인한다. Controller는 HTTP 입력·응답 변환을 맡고 application은 고객의 자원 소유권을 확인한다. 과제의 Spring Security 관리자 접근 설정을 사용한다. 관리자 허용, 일반 사용자·미식별 요청 거절을 검증한다. 경로·클래스 이름만으로 권한을 보장한다고 가정하지 않는다.
- 고객의 주문·포인트·좋아요 소유권은 application에서 확인한다. 입력 본문의 사용자 ID를 신뢰해 소유자를 결정하지 않는다.
- 고객 주문 조회와 관리자 조회는 `getMyOrder`와 `getAdminOrder`처럼 명시적인 메서드로 구분한다. `isAdmin` 플래그로 한 메서드의 조회 범위를 전환하는 구조를 피한다.
- 도메인에 관리자 여부나 Spring Security·HTTP 객체를 전달하지 않는다. 관리자도 같은 도메인 규칙을 따른다.
- 관리자 변경은 같은 DB의 고객 조회에 반영한다. 고객·관리자 응답 필드를 구분한다.
- 실습 실행은 `server.address=127.0.0.1`을 적용한다. 공개 배포와 실제 비밀정보·개인정보 사용을 하지 않는다. 모의 식별을 운영 인증으로 취급하지 않는다.

## 6. 테스트·lint·ArchUnit

- 신규 작성하거나 수정하는 커머스 테스트 메서드에는 JUnit의 `@DisplayName`으로 테스트 조건과 기대 결과를 한글로 명시한다. 예: `@DisplayName("재고보다 많은 수량을 차감하면 거절하고 기존 재고를 유지한다")`. 기존 Example 테스트는 이 규칙을 적용하기 위한 일괄 수정 대상에서 제외한다.
- 파라미터 테스트는 실패한 입력을 식별할 수 있도록 실행 이름에 입력값을 표시한다.
- 대표 도메인 규칙에는 TDD를 적용한다. 컴파일·DB 연결 실패를 규칙의 Red로 기록하지 않는다. 정상 사례와 거절 사례를 함께 검증한다.
- domain 테스트는 Spring·DB 없이 실제 Entity·VO로 수행한다. application 테스트는 출력 포트를 fake 또는 mock으로 대체할 수 있지만, 검증 대상인 도메인 행동은 대체하지 않는다.
- DB 통합 테스트는 `flush/clear` 후 재조회하여 실제 매핑·저장·제약을 확인한다. 트랜잭션 롤백은 실제 DB 테스트로 확인한다.
- HTTP 통합 테스트는 실제 Controller·application·저장 어댑터·테스트 DB를 연결한다. 관리자 테스트의 기본 보안 필터를 유지한다. domain에서 확인한 모든 경계값을 HTTP 테스트에 반복하지 않고 정상·대표 오류·접근 거절·저장 결과를 검증한다.
- 관리자 POST·PUT·DELETE 테스트와 권한 거절 테스트에는 유효한 CSRF 입력을 사용해 권한 오류와 구분한다.
- 충전 → 여러 품목 주문 생성·확정 → 주문·잔액 조회를 연결해 확인한다.
- Java 21과 기존 Gradle·Spring Boot 버전을 유지한다. Java lint는 Checkstyle, 의존 검사는 표준 ArchUnit을 사용한다.
- ArchUnit은 domain의 다른 계층·Spring·JPA 의존, application의 interfaces·infrastructure 의존, interfaces의 infrastructure 의존을 금지한다.
- 빈 application 검사 대상 허용은 최초 구현 전까지의 임시 예외다. 첫 신규 ApplicationService를 추가할 때 빈 대상 허용을 제거하고 실제 구현이 검사되는지 확인한다.
- 최종 검증에서는 신규 커머스 구현 클래스가 ArchUnit 검사 대상에 실제 포함되는지 확인한다. 빈 대상이나 자동 생성 클래스만 검사한 결과를 신규 구현의 검증 완료로 보고하지 않는다.

### 실행 기준

프로젝트 루트에서 관련 테스트를 먼저 실행하고, 기능 마무리 시 연결된 lint·ArchUnit 및 필요한 통합 검사를 수행한다.

- 다른 모듈에도 Java 코드가 있으면 Checkstyle 검사 범위를 확인하고, 필요한 연결 변경을 제안해 승인받는다.
- 수업용 CI가 있으면 아래와 같은 검사 명령을 연결하도록 제안하고 승인 후 반영한다.

```bash
# 실제 변경 기능의 테스트 이름으로 치환한다.
./gradlew :apps:commerce-api:test --tests '*Stock*Test'

# 패키지 의존 규칙 검사
./gradlew :apps:commerce-api:test --tests '*ArchitectureTest'

# 테스트·Checkstyle을 포함한 최종 검사
./gradlew :apps:commerce-api:check
```

### 결과 보고

- 마무리에는 변경 내용, 실행한 검사와 실제 결과, 미실행 이유 및 남은 정책을 구분해 보고한다. 실행 건수·실패·skip·종료 결과를 확인하고, 존재하지 않는 테스트나 실행하지 않은 검사를 통과했다고 쓰지 않는다.
- DB 테스트에 필요한 Docker 등 환경이 없다면 환경 문제와 코드 실패를 구분해 보고한다.
- 문서만 변경했다면 diff·내용 검증을 수행하고, 동작 테스트를 생략한 이유를 명시한다.
- 제출 전 구현·테스트·검사 설정·설계 문서·PR 설명·기술 글의 준비 여부를 확인한다. PR 설명에는 대표 TDD 사례의 Red → Green → Refactor 변화, 설계에서 바뀐 판단, 실행한 검사를 기록한다. 제출물 작성·수정도 변경 단위별 승인을 따른다.

## 7. 현재 구현의 임시 정책

사용자가 이번 연속 구현에서 미정 정책을 최소 필요 범위로 결정하도록 승인했다. 다음은 적용한 기준이며, 향후 변경에는 수정 전 승인 규칙을 따른다.

- 브랜드·상품 이름은 공백이 아닌 1~100자이며 설명은 받지 않는다.
- Money는 0 이상 long, 재고는 0 이상 int, 주문 수량은 양수 int다. 금액 연산 범위 초과는 거절한다.
- 주문은 1~100개 품목이며 중복 상품을 거절한다. 생성 시 단가 고정, 0원 주문 허용, 재확정 거절이다.
- 좋아요 중복 등록·없는 관계 취소, 브랜드·상품 반복 삭제는 성공으로 처리한다.
- 상품 latest는 생성 시각 내림차순이며 정렬 동률은 ID 내림차순이다. page=0, size=20(1~100)을 사용한다.
- 관리자 브랜드·상품 조회는 삭제 데이터를 포함한다. 고객 조회는 삭제 데이터를 제외한다.
- 실습 사용자는 fixture로 준비한다. Point는 최초 잔액 조회·충전 시 0원으로 생성한다.
- 구체적인 HTTP 계약과 잠금 전략은 `docs/week2/commerce-design.md`의 구현 정책을 따른다.

새로운 정책을 기존 합의로 취급하지 않는다. 이후 추가 판단은 당시 사용자의 승인 범위에 따라 제안한다.
