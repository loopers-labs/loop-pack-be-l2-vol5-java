server:
  # 로컬 실습용. 외부 인터페이스에 바인딩하지 않는다 — 인증이 없는 API 를 같은 망의 다른 기기가 부를 수 없게 한다.
  address: 127.0.0.1
  shutdown: graceful
  tomcat:
    threads:
      max: 200 # 최대 워커 스레드 수 (default : 200)
      min-spare: 10 # 최소 유지 스레드 수 (default : 10)
    connection-timeout: 1m # 연결 타임아웃 (ms) (default : 60000ms = 1m)
    max-connections: 8192 # 최대 동시 연결 수 (default : 8192)
    accept-count: 100 # 대기 큐 크기 (default : 100)
    keep-alive-timeout: 60s # 60s
  max-http-request-header-size: 8KB

spring:
  main:
    web-application-type: servlet
  application:
    name: commerce-api
  profiles:
    active: local
  config:
    import:
      - jpa.yml
      - redis.yml
      - logging.yml
      - monitoring.yml

springdoc:
  use-fqn: true
  swagger-ui:
    path: /swagger-ui.html

---
spring:
  config:
    activate:
      on-profile: local, test
  # 관리자 경로(/api-admin/**)를 실제로 호출하기 위한 로컬 전용 계정.
  # 비밀이 아니다 — 저장소에 그대로 들어가는 값이고, 이름으로 그 사실을 드러낸다.
  # 이 블록은 local·test 프로파일에만 있다. dev 아래로는 내려가지 않는다.
  security:
    user:
      name: admin
      password: local-only-not-a-secret
      roles: ADMIN

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: qa

---
spring:
  config:
    activate:
      on-profile: prd

springdoc:
  api-docs:
    enabled: false