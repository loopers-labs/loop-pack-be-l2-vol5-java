# CLAUDE.md

이 문서는 Claude가 이 저장소에서 개발 작업을 수행할 때 따라야 할 규칙이다.

## 0. 언어

한국어로 응답한다. 코드와 커밋 메시지의 기술 용어는 예외로 하되, 커밋 메시지 설명은 한글로 작성한다.

## 1. 과제 핵심 규칙 (최우선)

- 합의한 계약·기대값·패키지 의존을 따른다. 미정 정책은 먼저 질문한다.
- 이번 기능에서 변경할 책임·파일·관련 테스트를 먼저 제안한다.
- 작은 기능을 구현하고 diff와 관련 테스트·lint·ArchUnit 결과를 확인한다.
- 검사를 통과시키기 위한 테스트·기대값·규칙 삭제나 완화는 하지 않는다.
- 정책·검사 기준 변경이나 범위 밖 개편은 이유와 영향을 설명하고 확인을 받는다.

## 2. 코딩 전에 생각하라

- 추측하지 않는다. 가정(assumption)은 명시적으로 밝히고, 확실하지 않으면 질문한다.
- 여러 해석이 가능하면 전부 제시한다. 조용히 하나를 골라 진행하지 않는다.
- 더 단순한 접근이 있다면 먼저 말한다. 필요하면 반론을 제기한다.
- 수정 대상 코드는 반드시 먼저 읽는다. 읽지 않고 수정하지 않는다.
- 존재하지 않는 API·패키지·파일 경로·설정 옵션을 지어내지 않는다. 확실하지 않으면 먼저 확인한다.

## 3. 단순함 우선

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 불필요한 추상화를 만들지 않는다.
- 발생할 수 없는 시나리오에 대한 에러 핸들링을 하지 않는다.
- "시니어 엔지니어가 이걸 오버엔지니어링이라고 할까?"를 스스로 묻고, 그렇다면 단순화한다.
- 검증·실험 목적의 임시 코드(더미 클래스, 프로브 등)를 `src/main`에 만들지 않는다. 필요하면 먼저 제안하고 승인받는다.

## 4. 외과적 변경

- 건드려야 할 것만 건드린다. 요청 범위 밖의 코드·주석·포매팅을 "개선"하지 않는다.
- 고장나지 않은 것을 리팩토링하지 않는다.
- 기존 프로젝트 스타일(`interfaces`/`application`/`domain`/`infrastructure` 계층, 기존 `ApiResponse`/`ApiControllerAdvice` 등 공통 처리 방식)을 따른다.
- 관련 없는 데드코드를 발견하면 삭제하지 말고 언급만 한다.
- 변경으로 더는 쓰이지 않는 import·변수·함수는 제거한다.
- 변경된 모든 라인은 사용자 요청에 직접 연결되어야 한다.

## 5. 설계 기반 TDD

- 구현 전에 설계 문서의 계약과 주요 규칙의 기대값을 정한다.
- 대표 도메인 규칙은 Red → Green → Refactor로 구현한다.
    - Red: 정상·대표 오류의 기대값을 테스트로 먼저 작성, 의도한 실패 확인
    - Green: 테스트를 통과시키는 최소 구현
    - Refactor: 중복·이름·책임을 다듬고 테스트 재실행
- 계층별 테스트 전략:
    - 순수 도메인 규칙 → 단위 테스트
    - application → 요청자 구분과 객체 협력 검증
    - repository·DB → 저장 후 `flush()`/`clear()`로 재조회해 검증
    - HTTP → 실제 API 응답, 입력·권한 거절 시 기존 값 유지 검증
- 계층 의존은 ArchUnit으로, import 규칙은 Checkstyle로 검증한다.
- 구현 중 계약·책임·의존 판단이 바뀌면 설계 문서와 관련 테스트를 함께 갱신한다.

## 6. 점진적 실행

- 여러 파일을 한 번에 바꾸지 않는다. 작은 단위로 변경하고 그때마다 검증한다.
- 문제가 생겼을 때 원인을 특정할 수 있도록 변경 범위를 최소화한다.

## 7. 실패 대응

- 에러가 나면 증상만 고치지 말고 근본 원인을 분석한다.
- 같은 명령을 재시도하기 전에 왜 실패했는지 먼저 이해한다.
- 방향이 틀렸으면 매몰 비용에 집착하지 말고 과감히 다시 작성한다.
- 테스트·빌드 실패는 숨기지 말고, 실패 명령과 원인을 최종 요약에 기록한다.

## 8. 검증 명령

```bash
# 기능별 빠른 확인
./gradlew :apps:commerce-api:test --tests '*원하는키워드*Test'
./gradlew :apps:commerce-api:test --tests '*ArchitectureTest'

# 최종 검증 (Checkstyle 포함)
./gradlew :apps:commerce-api:check
```

## 9. 작업 절차

1. 요청과 관련된 코드·설정·테스트를 먼저 확인한다.
2. 변경 전 현재 동작과 의존성 경계를 파악한다.
3. 작은 단위로 구현하고 관련 테스트를 실행한다.
4. 필요하면 관련 모듈 전체 테스트와 빌드를 추가로 실행한다.
5. `git diff`로 의도하지 않은 변경이 없는지 확인한다.
6. 변경 내용, 실행한 검증, 남은 이슈를 최종 요약에 정리한다.

## 10. Git 규칙

- 사용자 요청 없이 커밋, 브랜치 생성, push, PR 생성, merge를 수행하지 않는다.
- 기존 변경사항과 무관한 파일은 건드리지 않는다.